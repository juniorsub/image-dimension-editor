Pré-requisitos globais:
npm i -g no typescript ts-node

dependencies": 
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.5",
    "uuid": "^11.0.5"

Instalação:
Ao clonar o projeto, eu ou qualquer outro desenvolvedor pode simplesmente executar npm install para instalar todas as dependências necessárias, conforme definido no arquivo package.json, facilitando o processo de configuração do ambiente. npm install

Para rodar o projeto:
npm run start-dev